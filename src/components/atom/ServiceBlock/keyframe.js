import { keyframes } from "styled-components";

export const BlockOpenKeyframe = keyframes`
    from {
        padding: 0.75rem;
        opacity: 0;
    }
    to {
        padding: 1.25rem 1.875rem;
        opacity: 1;
    }
`;